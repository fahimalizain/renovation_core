extend type Query {
  EventLogGetEvents(model: String!, name: String!): [EventLog!]!
  EventLogGetEventThread(parent_log: String!): [EventLog!]!
}

# extend type Mutation {
#   EventLogCreate()
#   EventLogDelete()
# }

type EventLog {
  name: String!
  event_type: String!
  entity_type: String!
  entity: String!
  content: String
  attachment: String
  created_by: String!
  primary_log: String
  parent_log: String
  ref_dt: String
  ref_dn: String
}
